# 安全政策

案齐保存的可能是高度敏感的案件、期限、收费和文件信息。请把安全配置、最小暴露和可靠备份视为部署的一部分。

## 支持范围

| 版本 | 安全更新 |
|---|---|
| 最新发布版本 | 支持 |
| 更早版本 | 不保证 |
| 未发布分支或自行修改版本 | 尽力协助定位，不承诺补丁 |

升级前请备份 SQLite 数据库与案件夹，并阅读对应 Release Notes。安全修复可能只在最新版本提供。

## 私密报告漏洞

不要为安全问题创建公开 Issue 或 Discussion，也不要把漏洞细节发送到普通需求表单。

请使用仓库的 [GitHub 私密漏洞报告](https://github.com/zj-ai-lab/anqi/security/advisories/new)，至少提供：

- 受影响版本、部署方式和必要配置；
- 最小复现步骤或概念验证；
- 实际与潜在影响；
- 你已经采取的临时缓解措施；
- 可以安全联系你的方式。

报告中不要附带真实案件数据、生产数据库、有效凭据、会话 token 或未做最小化处理的日志。若复现必须使用文件或数据库，请先说明所需结构，等待维护者确认安全传递方式。

维护者目标是在 7 个自然日内确认收到，并在 30 个自然日内给出初步判断；这是尽力目标，不构成服务等级承诺。修复和披露时间会根据影响、复现难度和发布风险协调确定。

## 测试边界

仅在你拥有或明确获准测试的部署上进行验证。未经单独书面授权，请勿：

- 测试维护者、其他用户或任何公开可访问的实例；
- 获取、修改或删除不属于你的数据；
- 进行拒绝服务、压力测试、批量扫描或社会工程；
- 建立持久化、横向移动或访问无关系统；
- 在修复发布前公开漏洞或可直接利用的细节。

发现意外数据访问时应立即停止、保留最少必要证据并私密报告。

## 部署者安全清单

- 设置强管理员密码，只保存版本化 `ANJIAN_PASS_HASH`，不保存明文；
- 使用 HTTPS 反向代理，不把应用端口直接暴露到公网；
- 明确配置 `ANJIAN_TRUST_PROXY`，不要信任不受控的转发头；
- 为 `/internal` 配置独立、高熵的 `ANJIAN_INTERNAL_KEY`，不要与登录密码或静态 token 复用；
- 限制 SQLite 数据库、案件夹、`.env` 和备份的文件系统权限；
- 备份加密并定期执行恢复演练；
- 不在日志、Issue、截图或监控系统中泄露凭据和当事人信息；
- 固定 Docker 版本标签并核对发布校验信息；
- 及时更新操作系统、Node.js、反向代理和依赖。

完整配置说明见 [SELF-HOSTING.md](SELF-HOSTING.md)。

## 不属于安全漏洞的事项

一般功能缺陷、法律规则争议、部署咨询和功能建议请使用对应 Issue 或 Discussion。某项配置本身不安全但文档已明确要求由部署者保护时，仍欢迎提出文档改进建议；请勿附带真实凭据或生产数据。
