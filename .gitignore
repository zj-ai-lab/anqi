node_modules/
# src/agent/assets/node_modules 是 supervisor.js 的 ensureAssetsNodeModulesLink()
# 在 dev 模式下每次 start() 前运行时按需创建/纠正的符号链接（指回 src/agent/
# runtime/node_modules，供 cordis 插件/mcp server.mjs 的 ESM import 解析）——
# 不提交进仓库；顶部的 `node_modules/` 规则按目录匹配，对这里的符号链接条目
# 不生效（git status 会把它当成未跟踪文件），所以单独钉一条精确路径规则。
# 打包模式下同名链接由 build/afterpack-agent-runtime-link.cjs 在 electron-
# builder 的 afterPack 阶段建在打包产物里（随 afterSign 一起签名封存），不
# 落在这棵仓库路径下，与这条规则无关。
/src/agent/assets/node_modules
data/
*.db
.env
.DS_Store

# 工具与临时产物
.playwright-mcp/
.workbuddy/
tmp/finance-ui-qa*/
*.png
!build/icon.png
!build/icon.iconset/*.png
!public/assets/anjian-icon.png
!public/assets/anjian-192.png
!public/assets/anjian-512.png
!public/assets/anjian-maskable-512.png
!docs/images/*.png
!garden-gpt-image-2/image/*.png
# Electron 构建产物（electron-builder 输出）
dist-electron/
# 文件桥联调沙盒
data/files-dev/

# Agent 工具与本地设置（可复原：npx skills add alchaincyf/huashu-design）
.agents/
.claude/
.zcode/
skills-lock.json
# 第三方设计参考素材（open-design，Apache-2.0）——设计已吸收进 DESIGN-TOKENS，素材不入库
docs/design-refs/
