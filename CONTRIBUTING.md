# 参与案齐

谢谢你点进来。案齐是我一个人写给自己用的案件管理工具，代码全部公开，你可以随便看、随便用、随便改——但主仓的代码，暂时只由我一个人动。下面说清楚为什么，以及你可以怎么参与。

## 为什么不收 Pull Request

案齐管的是律师手上真实的案件：程序节点、待办、当事人和法官的联系方式、律师费台账。它离业务太近，几乎每一处逻辑都对应着一个律师的实际工作方式——包括我自己的。这样的东西不适合做成一个谁都能往主线里改的项目：一个人觉得顺手的改动，可能正好打断另一个人的习惯，而这里的「不顺手」是要在办案中付代价的。所以主仓的代码，我自己写、自己看、自己担着。

另一个原因很现实：项目只有我一个维护者。收 PR 意味着评审、来回修改、合并后长期维护，这些我现在给不出足够的时间，与其让你的心血挂在那里等，不如一开始就说清楚。

所以：外部 PR 会被机器人礼貌关闭，我不会评审它。但这**只是维护方式，不是许可证限制**——AGPL-3.0-only 给你的权利一分不少：Fork、修改、运行、分发，只要你按许可证做。**想按自己的办案习惯定制，最好的办法就是 Fork 一份改成你的样子**；觉得某个改法对大家都好，开个 Issue 告诉我，我会认真看。

## 你可以怎么参与

- **发现问题** → 开 Bug report。给我最小可复现的步骤，脱敏后的日志。
- **有想法** → 开 Feature request，或到 Discussions 的 Ideas 聊。说清楚你遇到的**问题**和期望的**结果**，怎么实现交给我。
- **期限规则有误** → 这是我最需要的帮助。用「期限规则勘误」表单，附上法条原文和出处。这一类我会优先处理。
- **用不明白** → Discussions 的 Q&A。
- **发现安全漏洞** → 按 [SECURITY.md](SECURITY.md) 私下报我，别开公开 Issue。
- **想自己改** → Fork。

一个小请求：Issue 里描述问题就好，**不用贴成段的实现代码**。不是嫌弃，是因为案齐能一直保持现在这种「全公开、我一个人说了算」的状态，靠的是所有代码都出自我手，版权来源干净；哪天想换许可证、想做别的，不用问任何人。你贴了代码我照着用，这条线就模糊了。短的伪代码、接口示例、行为描述都没问题，只要是在解释问题而不是交付实现。

被我采纳的建议，会记在 [ACKNOWLEDGEMENTS.md](ACKNOWLEDGEMENTS.md) 里，署你的名。

## 缺陷报告标准

一份有效报告应尽量包括：

1. 案齐版本、运行方式（Docker、Node 或 macOS 桌面版）和操作系统；
2. 最小复现步骤；
3. 期望行为与实际行为；
4. 已脱敏的错误信息或日志片段；
5. 是否能在全新测试数据库中复现。

不要上传数据库、`.env`、案件夹、完整日志、浏览器存储、会话 cookie、API key、内网地址或未经脱敏的截图。

### 期限和法律规则问题

期限问题具有高风险。公开报告只能使用虚构事实，并应注明：

- 法域与程序类型；
- 触发事件和虚构日期；
- 计日方式、送达方式和顺延条件；
- 可公开核验的现行权威法源链接及具体条文。

Issue 中的结论不会自动进入规则库。`rules/deadline_rules.json` 的核准状态只能由维护者完成法律复核后变更；任何模型、自动化或社区意见都不能代替该核准。

## 隐私与脱敏

禁止提交真实当事人或案件事实，包括但不限于：

- 姓名、证件号、电话、地址、案号、账户和合同编号；
- 真实证据、文书、庭审安排、收费记录和聊天记录；
- 能通过组合信息重新识别个人或案件的细节。

示例统一使用张三、李四、`110101...`、`13800138000`、示例路 1 号等明显虚构数据。即使 Issue 随后删除，内容也可能已被缓存、通知或索引。

## 本地开发约束

如果你在自己的 Fork 中开发，建议先阅读：

- [docs/DESIGN.md](docs/DESIGN.md)：数据模型、期限引擎、LLM 边界和 API；
- [docs/DESIGN-TOKENS.md](docs/DESIGN-TOKENS.md)：三皮肤共骨架的 UI 约束；
- [SELF-HOSTING.md](SELF-HOSTING.md)：安全配置和运行方式。

基本检查命令：

```sh
npm ci
npm run check
```

前端修改还应在 `pro`、`paper`、`jade` 三种皮肤和窄屏下人工检查。期限引擎修改必须使用确定性测试，不得让 LLM 参与计算或生成最终期限。

## 行为要求

请就事论事、尊重他人，并默认公开讨论会被长期保存。维护者可以关闭包含敏感信息、重复内容、实质代码投稿、骚扰或与项目无关的 Issue 和 Discussion。
